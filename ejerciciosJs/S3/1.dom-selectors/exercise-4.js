const BestsPokemon = document.querySelectorAll('.pokemon');
console.log(BestsPokemon);

for (let FirstGeneration = 0; FirstGeneration < BestsPokemon.length; FirstGeneration++) {
    const element = BestsPokemon[FirstGeneration];
    console.log(element);

}